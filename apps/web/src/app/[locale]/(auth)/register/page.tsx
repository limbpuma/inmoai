import { RegisterForm } from "@/components/auth/RegisterForm";

export const metadata = {
  title: "Crear Cuenta - InmoAI",
  description: "Crea tu cuenta en InmoAI para acceder a todas las funcionalidades",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
